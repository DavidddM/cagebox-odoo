# Authentication — pick one:
#
# Option A (recommended): Leave ANTHROPIC_API_KEY blank, then after first start run:
#   docker exec -it claude-sandbox claude login
# This uses OAuth and works with a Pro/Max subscription. The token persists in the
# claude-config volume, so you only need to authenticate once.
#
# Option B: Set an API key from https://console.anthropic.com (pay-per-token,
# separate from any Pro/Max subscription).
ANTHROPIC_API_KEY=

# GitHub token for private repo access (optional)
# GITHUB_TOKEN=ghp_your_token_here

# Git identity (used for commits inside the sandbox)
GIT_USER_NAME=Your Name
GIT_USER_EMAIL=your@email.com

# Ports
ODOO_PORT=8069

# Database
ODOO_DB_HOST=db
ODOO_DB_PORT=5432
ODOO_DB_USER=odoo
ODOO_DB_PASSWORD=odoo
PG_PASSWORD=odoo
