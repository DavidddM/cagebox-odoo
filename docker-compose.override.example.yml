# Developer-specific overrides. Copy to docker-compose.override.yml and customize.
# This file is gitignored.

services:
  claude-sandbox:
    volumes:
      # === Project mounts (required) ===
      - ~/projects/my-addons:/workspace/my-addons
      - ~/projects/odoo:/workspace/odoo
      # - ~/projects/enterprise:/workspace/enterprise

      # === Odoo config (optional) ===
      # - ./config/odoo.conf:/etc/odoo/odoo.conf
      # - odoo-data:/var/lib/odoo

      # === Plugin & MCP auto-setup (optional) ===
      # Create from .example files in scripts/, then uncomment:
      # - ./scripts/setup-tools.sh:/scripts/setup-tools.sh:ro

# Uncomment if using odoo-data volume above:
# volumes:
#   odoo-data:
